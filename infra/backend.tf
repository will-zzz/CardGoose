# Remote backend configuration lives per environment in:
#   - envs/dev/versions.tf
#   - envs/prod/versions.tf
#
# Uncomment the `backend "s3" { ... }` block there after S3 state bootstrap (see BOOTSTRAP.md).
